{
  "name": "npm",
  "version": "1.0.0",
  "description": "Sistema de post its",
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /01-mis-post",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
